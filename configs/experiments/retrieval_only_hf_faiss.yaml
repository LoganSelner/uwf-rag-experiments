# Retrieval-only local baseline.
#
# Use this when you want to compare indexing and retrieval behavior without
# answer-generation variance.

extends: ../base.yaml

name: "retrieval_only_hf_faiss"
description: "Retrieval-only evaluation for the local HuggingFace + FAISS baseline"

query:
  generation:
    type: ""

  generation_llm:
    provider: ""
    model_name: ""
    temperature: 0.0

evaluation:
  mode: "retrieval_only"
