"""Reranker implementations.

Each reranker re-scores retrieved chunks and truncates to top_k.
Implementations will be added in future branches (Phase 3).

Planned implementations:
- Cross-encoder reranker (sentence-transformers CrossEncoder)
- ColBERT reranker
- Cohere reranker (API-based)

See ARCHITECTURE_PLAN.md Section 13, Phase 3.
"""
